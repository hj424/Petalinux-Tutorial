1. Create a RSA ssh key
$ ssh-keygen -o -t rsa -b 4096 -C "email@example.com"
// press ENTER for default settings
2. Paste the generated public key in your github/lab account

// Firing the problems:
// Also, the permission for id_isa.pub should be 600!
$ ssh-add -l
$ ssh-add
$ SSH_AUTH_SOCK=0 git clone ... // confirm that gnome-keyring is at fault on your system!

!!! ssh login without password:
https://www.shellhacks.com/ssh-login-without-password/
!!! ssh config
https://medium.com/tarkalabs/ssh-simplified-using-ssh-config-161406ba75d7
